import { ChangeDetectionStrategy, Component } from '@angular/core';

@Component({
  selector: 'app-auth-trigger',
  imports: [],
  templateUrl: './auth-trigger.html',
  changeDetection: ChangeDetectionStrategy.OnPush,
})
export class AuthTrigger { }
